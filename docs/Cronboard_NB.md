# 📘 Cronboard — Полная шпаргалка

## 📚 Содержание
1. [Что такое Cronboard](#что-такое-cronboard)
2. [Установка](#установка)
3. [Основы работы с cron](#основы-работы-с-cron)
4. [Запуск приложения](#запуск-приложения)
5. [Управление задачами](#управление-задачами)
6. [Горячие клавиши](#горячие-клавиши)
7. [Синтаксис cron-выражений](#синтаксис-cron-выражений)
8. [Примеры использования](#примеры-использования)
9. [Работа с удалёнными серверами](#работа-с-удалёнными-серверами)
10. [Специальные выражения](#специальные-выражения)
11. [Автодополнение путей](#автодополнение-путей)
12. [Советы и рекомендации](#советы-и-рекомендации)

---

## 1. Что такое Cronboard

**Cronboard** — это терминальное приложение для управления cron-задачами на локальных и удалённых серверах. Оно позволяет легко создавать, редактировать, удалять и отслеживать статус ваших запланированных задач.

### Основные возможности:
- ✅ Просмотр всех cron-задач в удобной таблице
- ✅ Создание новых задач с валидацией и понятным описанием
- ✅ Редактирование существующих задач
- ✅ Удаление задач
- ✅ Пауза и возобновление выполнения задач
- ✅ Автодополнение путей к файлам и скриптам
- ✅ Отображение последнего и следующего времени выполнения
- ✅ Подключение к удалённым серверам через SSH
- ✅ Управление задачами от имени другого пользователя (через sudo)

---

## 2. Установка

## Вариант 1: Установка через pip (вручную)
```bash
git clone https://github.com/antoniorodr/cronboard
cd cronboard
pip install .
```

Вариант 2: Homebrew (macOS/Linux)
```bash
brew tap antoniorodr/cronboard
brew install antoniorodr/cronboard/cronboard
```

Вариант 3: Используя uv
```bash
uv tool install git+https://github.com/antoniorodr/cronboard
```

Проверка установки
После установки проверьте, что cron установлен на вашей системе:
```bash
crontab -l
```
## 3. Основы работы с cron
Cron — это планировщик задач в Unix-подобных системах, который позволяет автоматически выполнять команды или скрипты в заданное время.

Структура cron-задачи
Каждая cron-задача состоит из двух частей:

Расписание — когда выполнять (cron-выражение)
Команда — что выполнять (путь к скрипту или команда)
Пример:
```bash
0 9 * * * /usr/bin/python3 /home/user/backup.py
```
Это означает: "Выполнять скрипт backup.py каждый день в 9:00"

Запуск приложения
После установки просто запустите:
```bash
cronboard
```
Вы увидите интерфейс с двумя вкладками:

Local — для управления задачами на локальной машине. 

Servers — для подключения к удалённым серверам

### 4 Управление задачами. 
Создание новой задачи

Нажмите клавишу c (create)

Появится диалоговое окно с тремя полями:

### Поле 1: Cron-выражение
```bash
* * * * *
```
Введите расписание в формате: минута час день месяц день_недели

Приложение автоматически покажет описание на понятном языке
### Поле 2: Команда
```bash
python3 /home/user/scripts/backup.p
```
Введите команду или путь к скрипту

Используйте Tab для автодополнения путей
### Поле 3: Идентификатор (ID)
```bash
backup-job-1
```
Дайте задаче уникальное имя для удобства

Нажмите Save для сохранения или Cancel для отмены

### Редактирование задачи

Выберите задачу в таблице (используйте стрелки или j/k)

Нажмите клавишу e (edit)

Измените нужные поля

Нажмите Save

### Удаление задачи
Выберите задачу в таблице
Нажмите клавишу D (delete, с заглавной буквы!)
Подтвердите удаление
### Пауза/возобновление задачи
Выберите задачу в таблице

Нажмите клавишу p (pause)

Статус изменится на "Paused" (красный цвет)

Нажмите p снова для возобновления
Обновление списка

Нажмите клавишу r (refresh) для обновления таблицы задач

### Горячие клавиши
Навигация
Клавиша | Действие  

j или ↓ | Переместиться на строку вниз  

k или ↑ | Переместиться на строку вверх  

h или ← | Переместиться на столбец влево 

l или → | Переместиться на столбец вправо 
 
Tab     | Переключение между панелями 

### Управление задачами
| Клавиша | Действие | 

 c | Создать новую задачу |

 e | Редактировать выбранную задачу |

 D | Удалить выбранную задачу (с заглавной!) |

 p | Поставить на паузу / возобновить задачу | 

 r | Обновить список задач |

### Общие
| Клавиша | Действие | 

Ctrl+Q | Выйти из приложения | 

Tab (в поле ввода) | Автодополнение пути |

### Синтаксис cron-выражений
Базовая структура
* * * * *
│ │ │ │ │
│ │ │ │ └─── День недели (0-7, где 0 и 7 = воскресенье)
│ │ │ └───── Месяц (1-12)
│ │ └─────── День месяца (1-31)
│ └───────── Час (0-23)
└─────────── Минута (0-59)
Специальные символы
| Символ | Значение | Пример | 

`    * | Любое значение | * * * * * = каждую минуту | 

`    , | Список значений | 0,30 * * * * = в 0 и 30 минут каждого часа | 

`   - | Диапазон значений | 0 9-17 * * * = каждый час с 9:00 до 17:00 |

`   / | Шаг (интервал) | */15 * * * * = каждые 15 минут |

### Подробные примеры
Символ * (звёздочка)
```
* * * * *
```
Означает "любое значение". Если все поля заполнены звёздочками — задача выполняется каждую минуту.

Символ , (запятая)
```
0,15,30,45 * * * *
```
Выполняется в 0, 15, 30 и 45 минут каждого часа.

Символ - (дефис)
```
0 9-17 * * 1-5
```
Выполняется каждый час с 9:00 до 17:00 по будням (понедельник-пятница).

Символ / (слэш)
```
*/10 * * * *
```
Выполняется каждые 10 минут (в :00, :10, :20, :30, :40, :50).

0 */6 * * *
Выполняется каждые 6 часов (в 00:00, 06:00, 12:00, 18:00).

## Примеры использования
#### Пример 1: Ежедневное резервное копирование
Задача: Запускать скрипт резервного копирования каждый день в 2:30 ночи

Cron-выражение:
```
30 2 * * *
```
Команда:
```
/usr/bin/python3 /home/user/scripts/backup.py
```
ID:
```
daily-backup
```
Описание: "Каждый день в 02:30"

#### Пример 2: Очистка временных файлов каждый час
Задача: Удалять временные файлы каждый час

Cron-выражение:
```
0 * * * *
```
Команда:
```
rm -rf /tmp/cache/*
```
ID:
```
hourly-cleanup
```
Описание: "Каждый час в :00"

#### Пример 3: Еженедельный отчёт
Задача: Отправлять отчёт каждый понедельник в 9:00

Cron-выражение:
```
0 9 * * 1
```
Команда:
```
/home/user/scripts/send_weekly_report.sh
```
ID:
```
weekly-report-monday
```
Описание: "Каждый понедельник в 09:00"

Примечание: День недели: 0 или 7 = воскресенье, 1 = понедельник, 2 = вторник, и т.д.

#### Пример 4: Проверка обновлений каждые 15 минут
Задача: Проверять наличие обновлений каждые 15 минут в рабочие часы

Cron-выражение:
```
*/15 9-17 * * 1-5
```
Команда:
```
/usr/bin/apt update
```
ID:
```
check-updates-workday
```
Описание: "Каждые 15 минут с 09:00 до 17:59 по будням"

#### Пример 5: Ежемесячная задача
Задача: Генерировать месячный отчёт в первый день каждого месяца в 00:00

Cron-выражение:
```
0 0 1 * *
```
Команда:
```
python3 /home/user/reports/monthly_report.py
```
ID:
```
monthly-report-generator
```
Описание: "В 00:00 первого числа каждого месяца"

#### Пример 6: Сложное расписание
Задача: Выполнять синхронизацию в определённые часы определённых дней

Cron-выражение:
```
0 8,12,18 * * 1,3,5
```
Команда:
```
/usr/local/bin/sync-data.sh
```
ID:
```
sync-mon-wed-fri
```
Описание: "В 08:00, 12:00 и 18:00 по понедельникам, средам и пятницам"

## Работа с удалёнными серверами
Cronboard позволяет управлять задачами на удалённых серверах через SSH.

#### Добавление сервера
Перейдите на вкладку Servers (используйте Tab для переключения)

Нажмите кнопку для добавления сервера

Заполните форму:

#### Поле Hostname:
```
192.168.1.100:22
```
Формат: hostname:port или ip:port

#### Поле Username:
```
admin
```
#### Поле Password:
```
ваш_пароль
```
Оставьте пустым, если используете SSH-ключ

#### Поле Crontab user (опционально):
```
www-data
```
Если нужно управлять задачами от имени другого пользователя (требуются sudo-права)

Нажмите Add Server

### Использование SSH-ключей
Если вы используете SSH-ключи для аутентификации:

Оставьте поле Password пустым

Убедитесь, что ваш публичный ключ добавлен на сервер

Cronboard автоматически использует ключи из ~/.ssh/

Файл known_hosts должен находиться в ~/.ssh/known_hosts

Управление задачами на удалённом сервере

После подключения к серверу:

Все горячие клавиши работают так же, как для локальных задач

Можно создавать, редактировать, удалять и приостанавливать задачи

Изменения сохраняются на удалённом сервере

## Специальные выражения
Вместо стандартного синтаксиса можно использовать специальные выражения:

| Выражение | Эквивалент | Описание | |-----------|------------|----------| 

 @reboot | — | При каждой загрузке системы |

 @yearly | 0 0 1 1 * | Один раз в год (1 января в 00:00) | 
 
 @annually | 0 0 1 1 * | То же, что @yearly |

 @monthly | 0 0 1 * * | Один раз в месяц (первого числа в 00:00) |
 
 @weekly | 0 0 * * 0 | Один раз в неделю (воскресенье в 00:00) | 
 
 @daily | 0 0 * * * | Один раз в день (в полночь) | 
 
 @midnight | 0 0 * * * | То же, что @daily |
 
 @hourly | 0 * * * * | Каждый час (в начале часа) |

### Примеры использования
#### Запуск при загрузке системы:
```bash
Выражение: @reboot
Команда: /home/user/startup.sh
ID: startup-script
```

#### Еженедельная очистка логов:
```bash
Выражение: @weekly
Команда: /usr/bin/find /var/log -type f -name "*.log" -mtime +30 -delete
ID: weekly-log-cleanup
```

### Ежедневная проверка дискового пространства:
```bash
Выражение: @daily
Команда: /home/user/scripts/check_disk_space.sh
ID: daily-disk-check
```

### Автодополнение путей
Cronboard имеет встроенное автодополнение для удобного ввода путей к файлам и скриптам.

Как использовать
При создании или редактировании задачи, в поле Command начните вводить путь

Нажмите клавишу Tab для активации автодополнения

Появится список файлов и папок

Используйте стрелки для выбора

Нажмите Tab снова для подтверждения выбора

### Пример работы автодополнения
#### Начальный ввод:
```bash
python3 /home/user/s
```
Нажимаете Tab — появляется список:
```bash
📁 scripts/
📄 settings.conf
📄 setup.py
```
Выбираете scripts/ — путь становится:
```
python3 /home/user/scripts/
```
Продолжаете вводить и снова Tab:
```
python3 /home/user/scripts/backup.py
```

Особенности автодополнения
Начальная директория: 

По умолчанию автодополнение начинается с домашней директории пользователя

Скрытые файлы: Файлы, начинающиеся с точки (.), скрыты по умолчанию

Множественные пути: Можно использовать автодополнение для команд с несколькими аргументами
```
cp /home/user/file1.txt /home/user/backup/
```
Советы и рекомендации
#### 1. Используйте абсолютные пути

❌ Плохо:
```
python script.py
```
✅ Хорошо:
```
/usr/bin/python3 /home/user/scripts/script.py
```
Почему: Cron выполняется в минимальном окружении, и относительные пути могут не работать.

#### 2. Перенаправляйте вывод в лог-файлы

Чтобы видеть результаты выполнения и ошибки:

/usr/bin/python3 /home/user/backup.py >> /home/user/logs/backup.log 2>&1
Объяснение:

` >> — добавляет вывод в конец файла (не перезаписывая)

`2>&1 — перенаправляет ошибки (stderr) в тот же файл, что и обычный вывод (stdout)

#### 3. Проверяйте права доступа
Убедитесь, что:

Скрипт имеет права на выполнение: chmod +x script.sh

У пользователя cron есть права на запись в лог-файлы

У пользователя cron есть права на выполнение команды

#### 4. Используйте осмысленные идентификаторы
#### 5. Тестируйте команды перед добавлением в cron
Сначала запустите команду вручную в терминале:
```
/usr/bin/python3 /home/user/scripts/backup.py
```
Убедитесь, что она работает корректно, и только потом добавляйте в cron.

#### 6. Используйте переменные окружения
Если вашему скрипту нужны переменные окружения, добавьте их в начало команды:
```
PATH=/usr/local/bin:/usr/bin:/bin && /home/user/script.sh
```
Или установите их в самом скрипте.

#### 7. Регулярно проверяйте статус задач
Используйте клавишу r для обновления списка
Проверяйте колонки Last Run и Next Run
Следите за статусом (Active / Paused / Inactive)

#### 8. Используйте специальные выражения для простоты
Вместо:
```
0 0 * * *
```
Используйте:
```
@daily
```
Это проще читать и понимать.

#### 9. Документируйте сложные расписания
Если используете сложные выражения, Cronboard автоматически показывает их описание:
```
Выражение: 0 8,12,18 * * 1,3,5
Описание: "В 08:00, 12:00 и 18:00 по понедельникам, средам и пятницам"
```
Это помогает избежать ошибок при редактировании.

#### 10. Используйте паузу вместо удаления
Если задача временно не нужна, используйте паузу (клавиша p) вместо удаления. 

Это позволит быстро возобновить её в будущем.


#### Ошибка: Задача не выполняется
Возможные причины и решения:

Задача на паузе

Проверьте статус (должен быть "Active", а не "Paused")

Нажмите p для возобновления
Неправильный путь к скрипту

Используйте абсолютные пути

Проверьте, что файл существует: ls /path/to/script.py

Нет прав на выполнение

Добавьте права: chmod +x /path/to/script.sh
Скрипт падает с ошибкой

Добавьте логирование: script.py >> /tmp/cron.log 2>&1

Проверьте логи после выполнения

#### Задача показывает "ERR" в колонке Next Run
Причина: Невалидное выражение или проблема с расписанием

Решение:

Отредактируйте задачу (клавиша e)

Проверьте корректность cron-выражения

Посмотрите на описание — оно подскажет, правильно ли понято выражение

### Быстрый старт: 5-минутное руководство

#### Шаг 1: Запустите Cronboard
```
cronboard
```

#### Шаг 2: Создайте первую задачу
Нажмите c 

Введите выражение: */5 * * * * (каждые 5 минут)

Введите команду:
```
date >> /tmp/crontest.log
```
Введите ID: 
```
test-job
```
Нажмите Save

#### Шаг 3: Подождите и проверьте
Подождите 5 минут и проверьте файл:
```
cat /tmp/crontest.log
```
Вы должны увидеть текущие дату и время.

#### Шаг 4: Поставьте задачу на паузу
Выберите задачу стрелками

Нажмите p

Статус изменится на "Paused"

#### Шаг 5: Удалите тестовую задачу
Убедитесь, что задача выбрана

Нажмите D (с заглавной буквы!)

Подтвердите удаление

### Заключение
Cronboard делает управление cron-задачами простым и интуитивным. 

Используйте эту шпаргалку как справочник, и вы быстро освоите все возможности приложения!


### Полезные ссылки
Репозиторий проекта: https://github.com/antoniorodr/cronboard

Сообщить о проблеме: https://github.com/antoniorodr/cronboard/issues

Поддержка проекта

Если вам нравится Cronboard, рассмотрите возможность поддержать разработчика:

GitHub Sponsors: https://github.com/sponsors/antoniorodr

